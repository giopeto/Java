package algorithms.tasks;
/*
*
* https://www.hackerrank.com/tests/sample/questions/3pramr7a684
*
* */
public class OddNumbers {

    // return IntStream.rangeClosed(l, r).filter(i -> i%2 != 0).boxed().collect(Collectors.toList());
}
